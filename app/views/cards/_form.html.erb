<section class="fader">
  <div class="close">
    <!-- <img src="https://i.ibb.co/N6bD89n/krest.png"/> -->
  </div>
  <section class="wrappersozd">
    <div class="shownew">
      <div class="zagolovokk">Новая карточка</div>

      <%= form_for(card) do |f| %>
        <% if card.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

            <ul>
            <% card.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <% if @topic %>
        <%= f.hidden_field :topic_id, value: @topic.id %>
        <% else %>

        <div class="nizsozd">
          <div class="vibortemi">
            <!-- <%= f.label :topic_id %> -->
            <%#= f.number_field :topic_id %>
            <%= select("card", "topic_id", Topic.all.collect { |t| [t.title, t.id] }, {:include_blank => 'Выбрать тему'}) %>
          </div>

        </div>

        <% end %>

        <div class="one_side">
          <div class="side_input">
            <!-- <%= f.label :question %> -->
            <%= f.text_field :question, placeholder: "Запишите вопрос...", autocomplete: "off" %>
          </div>
        </div>

        <div class="dobavlenieimage">
          <%= f.label :image, "+ Добавить изображение" %>
          <%= f.file_field :image %>
        </div>


        <!-- <div class="fieldd">
          <%= f.label :question %>
          <%= f.text_field :question, placeholder: "Добавьте вопрос..." %>
        </div> -->

        <div class="second_side">
          <div class="side_input">
            <!-- <%= f.label :answer %> -->
            <%= f.text_field :answer, placeholder: "Запишите ответ...", autocomplete: "off" %>
          </div>
        </div>

        <!-- <div class="fieldd">
          <%= f.label :answer %>
          <%= f.text_field :answer, placeholder: "Добавьте ответ..." %>
        </div> -->

        <div class="nizsozd">
          <div class="actions">
            <%= f.submit 'Сохранить', :class => 'sozdanie' %>
          </div>
        </div>




        <!-- <div class="fieldd">
          <%= f.label :memorization_level %>
          <%= f.number_field :memorization_level %>
        </div> -->



        <!-- <%= link_to card, method: :delete, data: { confirm: 'Are you sure?' } do -%>
          <div class="deleteee">Удалить карточку</div>
        <% end %> -->

      <% end %>

    </div>
  </section>
</section>
